The application does not bypass SSL hostname verification in production environments. Server identity validation is enforced using standard platform security mechanisms and trusted certificates. Any non-production/testing configurations are restricted to internal environments and are not included in release builds.
